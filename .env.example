# Backend API base URL (used by Axios api client)
# VITE_API_URL=http://localhost:3000/api

# External CRM API configuration
# Base URL of the CRM service (required for fetching Prospect contacts directly from the frontend)
VITE_CRM_API_URL=https://tu-crm.example.com/api

# API key or bearer token to authenticate with the CRM (if required)
VITE_CRM_EMAIL=InventarioUMG@miumg.edu.gt
VITE_CRM_PASSWORD=UMGinv2025$

# Optional: Path to the contacts resource in the CRM API
# Defaults to /contactos when not provided
# Example values: /contactos, /v1/contacts
# VITE_CRM_CONTACTS_PATH=/contactos

# Deprecated: API key (not used in the login flow)
# VITE_CRM_API_KEY=

# Optional: Custom contacts path if needed by other endpoints
# VITE_CRM_CONTACTS_PATH=/contactos
